import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { withDesign } from 'storybook-addon-designs'

import <%- props.name %>  from './<%- props.name %>.vue'

<Meta
  title="Componentes/<%- props.folder %><%- props.name %>"
  component={<%- props.name %>}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: 'figma',
      url: '',
    },
  }}
  argTypes={{
    label: 'hi!'
  }}
/>

export const Template = ({ label,...args }) => ({
  components: { <%- props.name %> },
  setup() {
    return { args, label }
  },
  template: `<<%- props.name %> v-bind="args" id="teste"></<%- props.name %>>`,
})

# <%- props.name %>

## Melhores praticas


## Acessibilidade


## Importar localmente

```js dark
import { Pea<%- props.name %> } from '@pescarte/boia'
```

## Variants 
<Canvas>
  <Story
    name="Default"
    args={{
      label: 'default',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Exemplos de uso

```html dark
<Pea<%- props.name %> id="just-test">Awesome component!</Pea<%- props.name %>>
```

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{
      label: 'its work fine!',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
