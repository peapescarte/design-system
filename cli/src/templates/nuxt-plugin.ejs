import { defineNuxtPlugin } from '#app';

import { install as boia } from '@pescarte/boia'
import '@pescarte/boia/dist/style.css'

export default defineNuxtPlugin(({ vueApp }) => {
  vueApp.use(boia)
})