import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { withDesign } from 'storybook-addon-designs'

import InputText  from './InputText.vue'

<Meta
  title="Componentes/forms/inputsInputText"
  component={InputText}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: 'figma',
      url: '',
    },
  }}
  argTypes={{
    label: 'hi!'
  }}
/>

export const Template = ({ label,...args }) => ({
  components: { InputText },
  setup() {
    return { args, label }
  },
  template: `<InputText v-bind="args" id="teste"></InputText>`,
})

# InputText

## Melhores praticas


## Acessibilidade


## Importar localmente

```js dark
import { PeaInputText } from '@pescarte/design-system'
```

## Variants 
<Canvas>
  <Story
    name="Default"
    args={{
      label: 'default',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Exemplos de uso

```html dark
<PeaInputText id="just-test">Awesome component!</PeaInputText>
```

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{
      label: 'its work fine!',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />
